
public class GEmployee {
    private String IDNumber;
    private String name;
    private String residentNumber;
    private String password;

    //생성자. 내부변수 초기화를 진행
    GEmployee(String IDNumber, String name, String residentNumber){
        ~~Initial reset;
    }

    //패스워드 설정 메소드. 아마 부모클래스로 들어갈 것
    public void changePassword(String password) {

    }

    //이 메소드에서 강의 정보들을 입력 받아서, Lecture 클래스를 만든 뒤 리턴한다.
    public Course addCourse(){


        return new Course();
    }

    //청구서 출력 메소드
    public void printBill(Main headClass){
        try{
            print("청구서를 전송할 강의 번호를 입력해주세요 : ");
            int courseNum = parseInt(getConsoleInput);

            Stream searchResult = headClass.courseArray에서 CourseNum과 같은 번호를 가진 객체 검색

            if(searchResult.count!=1){
               println("오류가 발생했습니다. 강의가 존재하지 않을 수 있습니다.");
               println("해당 오류가 지속적으로 발생 시, 관리자에게 문의하시기 바랍니다.");
            }

        }


        try{
            tgtCourse = headClass.courseArray[index];

            for(int i : tgtCourse.student){
                headClass.studentsArray[i];


            }
        }

        catch(NullPointerException e){

        }
    }

    //콘솔 입력을 리턴하는 함수
    String getConsoleInput() throws IOException{
        BufferedReader를 사용한 콘솔 입력

        return inputString;
    }

}